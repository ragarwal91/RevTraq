App.Models.Sale = Backbone.Model.extend({

  initialize: function() {
    console.log('sale model init');  
  }

  // urlRoot: '/businesses/:id/sales',
  // initialize: function() {
  //   this.businesses = new App.Collections.BusinessesCollection();
  //   this.businesses.url = '/businesses/' + this.id + '/sales';
  // }
  
});